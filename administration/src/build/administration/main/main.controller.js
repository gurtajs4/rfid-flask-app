(function () {
    'use strict';

    angular
        .module('appMain')
        .controller('MainController', MainController);

    MainController.$inject = ['$scope'];
    function MainController($scope) {
    }
})();